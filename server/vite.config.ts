import { defineConfig } from 'vite'

export default defineConfig({
  // This config is now mainly for Vitest, which will automatically pick it up.
  // We can add test-specific configurations here later if needed.
})
